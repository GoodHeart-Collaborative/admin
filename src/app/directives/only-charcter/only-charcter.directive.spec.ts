import { OnlyCharcterDirective } from './only-charcter.directive';

describe('OnlyCharcterDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyCharcterDirective();
    expect(directive).toBeTruthy();
  });
});
